eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: false
  #关掉保护模式（开发环境关闭，生产环境要打开）
  server:
    enable-self-preservation: false
spring:
  application:
    name: eureka
server:
  port: 8761
